# Dependencies
node_modules/

# Build output
dist/

# Wrangler
.wrangler/
.wrangler-test/
# Private per-operator deploy config (real resource ids); never commit
wrangler.deploy.toml

# Environment
.env*
!.env.example
.dev.vars*
!.dev.vars.example

# Logs
*.log

# Test
test-results/

# OS
.DS_Store

# IDE
.claude/

# Generated build identity (stamped at build/deploy by scripts/stamp-build.mjs)
src/build-info.ts

# One-time price backfill runtime state (resume cursor + scratch SQL)
tools/price-backfill/.cursor.json
tools/price-backfill/.batch.sql

# Secrets and keys (belt and suspenders; never commit any of these)
*.pem
*.key
*.p8
*.local
.secrets
secrets
secrets.*
